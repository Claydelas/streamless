bool skip = true;
while(read(0) != null){
    if(skip){
        consume(0);
        consume(0);
        skip = false;
    }else{
        consume(0);
        consume(0);
        consume(0);
        skip = true;
    }
    if(read(0) != null){
        println(get(0));
    }
}
